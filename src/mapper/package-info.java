/**
 * Provides needed Classes used as sql connection wrappers
 * <p>
 * Mappers provide an easy to use way of working with sql connections. While
 * being fairly more limited than classic Prepared Staments (Mappers actually
 * use them) makes database manipulation code easier to read and faster to
 * write.
 */
package mapper;
